image:
  repository: baothaihcmut/ecommerce-users-service
  tag: v1
  pullPolicy: Always
configMap:
  name: main
  data:
    SERVER_PORT: "8080"
    RABBITMQ_URI: "amqps://nvfpayhh:X7H1EaS5XkBgNAxG9oa2FhrsDT6SVkho@possum.lmq.cloudamqp.com/nvfpayhh"
    REDIS_HOST: "redis-13770.c321.us-east-1-2.ec2.redns.redis-cloud.com"
    REDIS_PORT: "13770"
    REDIS_PASSWORD: "Tazqqr2OljF2IGBJQ5pkz8RAbbpSCGkN"
    REDIS_USERNAME: "default"

config:
  from: "env"
  envConfigMap:
    - "main"
service:
  name: "users-service"
  type: ClusterIP
  ports:
    - name: "http"
      protocol: "TCP"
      port: 80
      targetPort: 8080
resources:
  requests:
    cpu: "0.4"
    memory: "256Mi"
  limits:
    cpu: "0.8"
    memory: "512Mi"
serviceAccount:
  enabled: true
  name: wait-dependency
  rules:
    - apiGroups: [""]
      resources: ["pods"]
      verbs: ["get", "list", "watch"]

initContainers: []
  # - name: wait-for-dependencies
  #   image: bitnami/kubectl:latest
  #   command:
  #     - /bin/sh
  #     - -c
  #     - |
  #       echo "Waiting for Redis and Kafka to be ready..."
  #       kubectl wait --for=condition=Ready pod -l app=ecommerce-app-dependency --timeout=300s






